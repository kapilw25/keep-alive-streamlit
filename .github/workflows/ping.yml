name: Ping Streamlit App

on:
  schedule:
    - cron: '0 9 * * *'  # Runs daily at 2 AM PST (9 AM UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your Streamlit app
        run: curl https://text2image-bykapil.streamlit.app/
